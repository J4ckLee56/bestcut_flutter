# GPT 기반 요약 파이프라인 설계

## 문제점
- GPT는 긴 내용을 한 번에 처리할 때 앞부분에만 집중하는 문제
- 토큰 제한으로 인한 내용 손실
- 전체 맥락 파악 어려움

## 해결 방안: 계층적 청크 기반 처리

### Phase 1: 로컬 전처리 (GPT 호출 전)
1. **의미 단위 구성**: 세그먼트를 의미 단위로 재구성
2. **중요도 계산**: TF-IDF + 중심성 기반 중요도 계산
3. **기본 챕터 분할**: 로컬 알고리즘으로 챕터 경계 파악
4. **청크 크기 최적화**: 각 챕터를 GPT가 처리하기 적절한 크기로 분할

### Phase 2: 챕터별 GPT 처리 (독립적 처리)
각 챕터를 **독립적으로** GPT에 전달하여 다음을 수행:

#### 2-1. 챕터 내용 분석
- **입력**: 챕터 내 세그먼트들 (최대 50-100개 세그먼트)
- **요청 내용**:
  ```
  1. 이 챕터의 주요 주제와 핵심 내용 파악
  2. 챕터 내에서 가장 중요한 내용이 시작하는 지점과 끝나는 지점 식별
  3. 범위 내 불필요하거나 중요도가 낮은 세그먼트 ID 목록
  4. 반복되는 내용이 있다면 가장 신뢰도 높은 세그먼트 ID
  ```
- **출력 형식**: JSON
  ```json
  {
    "chapter_index": 1,
    "main_topic": "주요 주제",
    "key_points": ["핵심 포인트 1", "핵심 포인트 2"],
    "important_range": {
      "start_segment_id": 45,
      "end_segment_id": 78,
      "reason": "이 범위에서 핵심 내용이 논의됨"
    },
    "exclude_segments": [52, 65, 71],
    "repetition_groups": [
      {
        "segments": [48, 55, 62],
        "keep": 48,
        "reason": "가장 명확하고 완전한 표현"
      }
    ],
    "summary": "챕터 요약 텍스트"
  }
  ```

#### 2-2. 청크 크기 제한
- **최대 세그먼트 수**: 80-100개 (약 5-10분 분량)
- **초과 시**: 챕터를 하위 청크로 분할
- **각 청크 독립 처리**: 각 청크를 별도로 GPT에 전달

#### 2-3. 청크 간 맥락 유지
- **이전 청크 요약 포함**: 이전 청크의 요약을 다음 청크 프롬프트에 포함
- **전체 구조 정보 제공**: 전체 챕터 구조를 간략히 제공

### Phase 3: 챕터별 결과 통합
1. **중요 범위 통합**: 각 청크의 중요 범위를 통합
2. **제외 세그먼트 통합**: 모든 청크에서 제외된 세그먼트 수집
3. **반복 내용 통합**: 모든 청크의 반복 그룹 통합
4. **챕터 요약 통합**: 각 청크 요약을 결합하여 챕터 전체 요약 생성

### Phase 4: 전역 최적화 (선택적)
1. **챕터 간 반복 제거**: 챕터 간에도 반복되는 내용이 있는지 확인
2. **전체 흐름 검증**: 선택된 세그먼트들이 자연스러운 흐름을 만드는지 확인
3. **최종 조정**: 필요시 세그먼트 추가/제거

## 구현 세부사항

### GPT 호출 최적화
1. **프롬프트 설계**:
   - 명확한 지시사항
   - JSON 형식 강제
   - 예시 제공

2. **청크 크기 관리**:
   - 세그먼트 수 기준: 80-100개
   - 시간 기준: 5-10분
   - 토큰 수 추정: 약 8,000-12,000 토큰

3. **에러 처리**:
   - 재시도 로직
   - 부분 실패 시 로컬 폴백
   - 진행 상황 저장

### 비용 최적화
1. **필요한 부분만 GPT 호출**:
   - 로컬로 후보 선정 후 GPT에 전달
   - 불필요한 세그먼트는 제외

2. **청크 크기 최적화**:
   - 너무 작으면 비용 증가
   - 너무 크면 품질 저하
   - 적절한 균형점 찾기

3. **캐싱**:
   - 동일 챕터 재처리 방지
   - 부분 결과 저장

## 파이프라인 흐름도

```
[로컬 전처리]
  ↓
의미 단위 구성
  ↓
중요도 계산
  ↓
기본 챕터 분할
  ↓
[GPT 처리 - 챕터별 독립 처리]
  ↓
챕터 1 → GPT → 결과 1
챕터 2 → GPT → 결과 2
챕터 3 → GPT → 결과 3
...
  ↓
[결과 통합]
  ↓
중요 범위 통합
제외 세그먼트 통합
반복 내용 통합
챕터 요약 통합
  ↓
[전역 최적화]
  ↓
최종 요약 세그먼트 선정
```

## 예상 비용
- 챕터당 약 1회 GPT 호출
- 6개 챕터 기준: 약 6회 호출
- 각 호출당 약 10,000 토큰 (입력) + 500 토큰 (출력)
- 총 약 63,000 토큰
- gpt-4o-mini 기준: 약 $0.03-0.05

## 장점
1. ✅ GPT가 전체 내용을 놓치지 않음 (청크별 독립 처리)
2. ✅ 앞부분 집중 문제 해결 (각 청크가 독립적)
3. ✅ 비용 최적화 (필요한 부분만 처리)
4. ✅ 품질 향상 (맥락을 고려한 정확한 판단)
5. ✅ 확장성 (긴 영상도 처리 가능)

## 단점
1. ⚠️ 처리 시간 증가 (여러 번 GPT 호출)
2. ⚠️ 비용 발생 (하지만 최적화 가능)
3. ⚠️ 네트워크 의존성




